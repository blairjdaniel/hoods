export default function Footer(){
  return (
    <footer style={{padding:24,marginTop:40,opacity:0.8,textAlign:'center'}}>
      © {new Date().getFullYear()} Hoods DAO — Built with ❤️
    </footer>
  );
}
